// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'hmusic.blob.core.windows.net',
      '1.bp.blogspot.com', 
      'localhost:3001', 
      'i1.sndcdn.com', 
      'www.clipartkey.com'
    ],
  },
};

export default nextConfig;
